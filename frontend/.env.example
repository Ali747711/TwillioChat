# Optional. Leave unset for normal use: the app calls /api same-origin and the
# Vite dev proxy (see vite.config.ts) forwards to the backend on :3001.
# Only set this to an absolute URL if you run the backend somewhere else.
# VITE_API_BASE=
